{
  "scripts": {
    "dev:browser-sync": "browser-sync start --server --files \"**/*.html, **/*.css\"",
    "dev:tailwindcss": "npx tailwindcss -i ./src/style.css -o ./dist/style.css --watch",
    "dev:copy-img": "cp -r ./assets/img ./dist/",
    "dev": "npm-run-all -p dev:*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.3.3"
  }
}
